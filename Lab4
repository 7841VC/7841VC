#include <iostream>
using namespace std;
 
int main()
{
    int i=0;
    string s, s1;
    cout << "String:\n";
    getline(cin, s);
 
    while(s[i])
    {
       if(s[i]<'0' || s[i]>'9') s1+=s[i];           
       i++;
    }
    s=s1;
    cout << s << "\n";
system("pause");
return 0;
}
